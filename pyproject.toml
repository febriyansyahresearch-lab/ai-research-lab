[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.backends._legacy:_Backend"

[project]
name = "ai-research-lab"
version = "1.0.0"
description = "Academic research: AI/ML/DL/CV/RL for cybersecurity applications"
requires-python = ">=3.10"
